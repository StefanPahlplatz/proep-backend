language: java

jdk:
  - oraclejdk8

services:
  - docker

jobs:
  include:
    - stage: test
      script: 
        - ./mvnw package
    - stage: deploy
      script:
        - echo `date`
      branches:
        only: master

